{
  "name": "opensquad-project",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-agent orchestration framework — create AI squads that work together",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "publish-carousel": "node squads/insta-project-carousel/scripts/publish-carousel.js"
  },
  "dependencies": {
    "playwright": "^1.58.2",
    "puppeteer": "^22.0.0",
    "axios": "^1.6.0",
    "form-data": "^4.0.0",
    "dotenv": "^16.0.0"
  }
}
